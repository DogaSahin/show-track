# Module-wide default for every Robolectric test class here, so a second test class doesn't
# silently fall back to targetSdk and fail with the same unexplained "API level 36 is not
# available". See LibraryDaoTest's class doc for why 35, not this module's compileSdk (36).
sdk=35
